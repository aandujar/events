
export class SalesDates {
    startDateTime: string
    endDateTime: string
  
    constructor(
        startDateTime: string,
        endDateTime: string
    ) {
      this.startDateTime = startDateTime
      this.endDateTime = endDateTime
       }
  }