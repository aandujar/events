export class Page {
    number: number
    totalElements: number
    totalPages: number
  
    constructor(number: number, totalElements: number, totalPages: number) {
      this.number = number
      this.totalElements = totalElements
      this.totalElements = totalElements
      this.totalPages = totalPages
    }
  }